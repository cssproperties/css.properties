import { propertySelectorTemplate } from "./property-selector.template.js";
import { propertySelectorStyles as styles } from "./property-selector.styles.js";

export const definition = Object.freeze({
    name: "property-selector",
    template: propertySelectorTemplate(),
    styles,
});
